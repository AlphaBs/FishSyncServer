@model BucketsViewModel

<h3 class="mb-3">Buckets</h3>
<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Owners</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var bucket in Model.Buckets)
        {
            <tr>
                <th>bucket.Id</th>
                <th>me</th>
            </tr>
        }
    </tbody>
</table>